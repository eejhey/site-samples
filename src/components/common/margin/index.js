import React from 'react';

export default function Margin(props) {
    return (
        <div
            style={{
                width: props.width,
                height: props.height,
            }} />
    )
}